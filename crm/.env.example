CHANNEL_SERVICE_URL=http://localhost:3001
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: enables real Gemini-generated AI suggestions (segments, message
# copy, channel recommendation). Without it, the /api/ai/suggest endpoint
# falls back to deterministic rule-based suggestions.
GEMINI_API_KEY=
